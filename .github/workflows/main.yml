on:
  workflow_dispatch:

jobs:
  build-devtools:
    runs-on: ubuntu-latest
    steps:
    - name: setup-depot-tools
      uses: newkdev/setup-depot-tools@v1.0.1
    - name: Create gclient config
      run: gclient config --unmanaged https://chromium.googlesource.com/devtools/devtools-frontend
      working-directory: ../
    - name: Rename devtools-frontend-docs
      run: mv devtools-frontend-docs devtools-frontend
      working-directory: ../
    - name: Sync dependencies
      run: gclient sync
      working-directory: $HOME/runner/work/devtools-frontend-docs/devtools-frontend
    - name: Generate build files (gn)
      run: gn gen out/Default
      working-directory: $HOME/runner/work/devtools-frontend-docs/devtools-frontend
    - name: Build DevTools Frontend (autoninja)
      run: autoninja -C out/Default
      working-directory: $HOME/runner/work/devtools-frontend-docs/devtools-frontend
